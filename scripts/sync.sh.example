#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$PROJECT_ROOT"

PYTHON_BIN="${FIREAGENT_PYTHON:-python}"
exec "$PYTHON_BIN" -m mcp_server.cli sync "$@"
